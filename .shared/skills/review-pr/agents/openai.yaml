interface:
  display_name: "Review PR"
  short_description: "Review a branch diff and write a markdown report"
  default_prompt: "Use $review-pr to review the current branch against its merge base and write the report under reviews/<project>/."
